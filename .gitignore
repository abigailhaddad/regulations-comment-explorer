.DS_Store
__pycache__/
.wrangler/
.playwright-mcp/

# Rebuilt by build_data.py / CI; 8.7 MB between them, and changes daily.
# Globbed rather than named, so a schema bump does not start committing data.
web/data/dockets.v*.json
web/data/titles.v*.json
